.env
.venv/
__pycache__/
*.pyc
.DS_Store

# IPA exam artifacts — original and derived — are NOT redistributed via this repo.
# See README for how to regenerate locally.
download/
ocr_out/
dataset/

# Supabase CLI scratch state (project refs, versions, pooler URL)
supabase/.temp/
supabase/.branches/
