.env
.venv/
__pycache__/
*.pyc
.DS_Store

# IPA exam artifacts — original and derived — are NOT redistributed via this repo.
# See README for how to regenerate locally.
download/
ocr_out/
dataset/

# Supabase CLI scratch state (project refs, versions, pooler URL)
supabase/.temp/
supabase/.branches/

# Claude Code per-project state (skills, agents, sessions, plans, chat hist)
.claude/

# Xcode per-user state and build artifacts
xcuserdata/
*.xcuserstate
DerivedData/
.swiftpm/
.build/
build/
*.moved-aside

# Don't commit .p8 or .pem keys that may end up next to the project
*.p8
*.pem
.vercel
